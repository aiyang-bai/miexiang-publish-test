# miexiang-publish-test

This public repository is used for real provider end-to-end tests of `miexiang-generic-publish`.

Release assets and manifests are disposable verification fixtures for the `test` channel. This repository does not contain product source code, end-user release binaries, credentials, or user data.